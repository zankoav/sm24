include ./../mixins
doctype html
html
    +head()
    body(class="body_overflow_hidden")
        script
            include ./../utils/_landing_ajax.js
        +header(false)
        main.home
            +repair()
            +advantages()
            +whatWeDo()
            +workOrder()
            +destruction()
            +marks()
            - var img_1 = require("@img/item_3.png");
            - var img_2 = require("@img/item_4.png");
            +textContent("Можно ли отремонтировать машинку в Минске по приемлемым ценам?", img_1)
            +textContent("95% ремонтов стиральных машин производится на дому", img_2, true)
        +footer()
        +modal()
            